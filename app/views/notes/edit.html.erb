<div class="notes-new-container">
    <h1>Edit Note</h1><br><br>
    <div class="form-group">
        <%= form_for(@note) do |f| %>
            <%= f.label "Title: " %>
            <%= f.text_field :title, class: "form-control" %>
            <%= f.label "Body: " %>
            <%= f.text_area :body, size: "30x10", class: "form-control" %>
            <br>
            <%= f.submit "Edit note", class: "btn btn-info" %>
        <% end %>
    </div>
</div>

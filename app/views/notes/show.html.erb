<h1><%= @note.title %></h1>
<h2><%= @note.created_at %></h2>

<p><%= @note.body %></p>

<%= button_to "Edit Note", edit_note_path(@note), method: :get, class: "btn btn-primary"%>
<br>

<%= button_to "Delete Note", note_path(@note), method: :delete, class: "btn btn-primary"%>
